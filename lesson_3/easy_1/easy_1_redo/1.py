numbers = [1, 2, 3]
numbers[6] = 5

# Will the code below raise an error?

# Yes, this code will raise an exception. When `6` is used as the index
# position of `numbers`, since that index does not exist, it raises an
# `IndexError`.